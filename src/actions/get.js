export const getTitle = (title) => ({
    type: 'GET_TITLE',
    title
});

export const getAlbums = (albums) => ({
    type: 'GET_ALBUMS',
    albums
});

export const getPhotos = (photos) => ({
    type: 'GET_PHOTOS',
    photos
});