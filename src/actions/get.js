export const getUsername = (username) => ({
    type: 'GET_USERNAME',
    username
});

export const getTitle = (title) => ({
    type: 'GET_TITLE',
    title
});

export const getAlbums = (albums) => ({
    type: 'GET_ALBUMS',
    albums
});